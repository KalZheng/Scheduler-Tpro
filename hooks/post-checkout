#!/bin/sh
BRANCH=$(git rev-parse --abbrev-ref HEAD)
ENV_FILE=".env.$BRANCH"

if [ -f "$ENV_FILE" ]; then
  cp "$ENV_FILE" .env
  echo "✅ Loaded $ENV_FILE -> .env"
else
  echo "⚠️  No $ENV_FILE found for branch '$BRANCH' — .env left unchanged"
fi